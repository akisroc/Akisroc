<!DOCTYPE html>
<html>

  <head>
    <meta name="language" content="fr-FR" />
    <meta charset="UTF-8">
    <title>{% block title %}Akisroc{% endblock %}</title>
    {% block stylesheets %}
      <link rel="stylesheet" href="{{ asset('build/css/app.css') }}">
    {% endblock %}
    <link rel="icon" type="image/png" href="favicon.png" />
  </head>

  <body>

    {# HEADER #}
    <header>
      <div id="navigation">
        <label for="menu-toggle" role="button">
          <span class="fas fa-compass"></span>
        </label>
        <input type="checkbox" id="menu-toggle" hidden aria-hidden="true">
        <nav>
          {% if app.user %}
            <a href="#">{{ app.user }}</a>
            <a href="{{ path('logout') }}">Se déconnecter</a>
          {% else %}
            <a href="{{ path('login') }}">Se connecter</a>
            <a href="{{ path('user.register') }}">S'inscrire</a>
          {% endif %}
        </nav>
      </div>
{#      <div class="header-title">#}
{#        <a href="{{ path('homepage') }}">#}
{#          <h1 class="cell">#}
{#            Akisroc#}
{#          </h1>#}
{#        </a>#}
{#      </div>#}
    </header>
    {# END HEADER #}

    {# CONTENT #}
    <main>
      {% block body %}{% endblock %}
    </main>
    {# END CONTENT #}

    {# FOOTER #}
    <footer>
    </footer>
    {# END FOOTER #}

    {# SCRIPTS #}
    {% block javascripts %}
      <script src="{{ asset('build/js/app.js') }}"></script>
    {% endblock %}
    {# END SCRIPTS #}

  </body>

</html>
